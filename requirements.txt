# ============================================================
# V2 EPF Pipeline — Python Dependencies (Fully Pinned)
# Probabilistic Electricity Price Forecasting
# PJM + ERCOT, 2019–2025
# ============================================================
# ALL versions pinned for full reproducibility.
# Run: pip install -r requirements.txt
# Verify: pip check && python smoke_test.py

# ── Core data handling ───────────────────────────────────────
pandas==2.3.3
numpy==2.4.4
pyarrow==24.0.0
scipy==1.15.3

# ── Data acquisition ─────────────────────────────────────────
gridstatus==0.36.0
requests==2.34.0

# ── Feature selection ────────────────────────────────────────
scikit-learn==1.8.0
shap==0.51.0

# ── Statistical baselines ─────────────────────────────────────
statsforecast==2.0.3
holidays==0.96

# ── Tree-based models ────────────────────────────────────────
lightgbm==4.6.0
xgboost==3.2.0
quantile-forest==1.4.1
joblib==1.5.3

# ── Deep learning (TensorFlow stack) ─────────────────────────
tensorflow==2.21.0
tensorflow-probability==0.25.0

# ── Deep learning (PyTorch / NeuralForecast) ─────────────────
torch==2.11.0
neuralforecast==3.1.8

# ── Foundation model ─────────────────────────────────────────
chronos-forecasting==2.2.2

# ── Conformal prediction ─────────────────────────────────────
mapie==1.4.0

# ── Evaluation ───────────────────────────────────────────────
properscoring>=0.1         # Single stable release, no versioned updates
arch==8.0.0

# ── Visualization ────────────────────────────────────────────
matplotlib==3.10.9
seaborn==0.13.2

# ── Utilities ────────────────────────────────────────────────
tqdm==4.67.3
python-dateutil==2.9.0.post0
pytz==2026.2
